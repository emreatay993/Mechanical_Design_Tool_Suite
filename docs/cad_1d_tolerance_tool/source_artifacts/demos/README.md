# Demo Videos

Raw demo videos are intentionally not tracked in Git.

Store recordings locally or in an approved external location, then add an entry to `../manifest.md`. If a recording is transcribed, place the transcript in `../transcripts/` and captions in `../captions/` when they are safe to track.
